// SeriesList.jsx

import React from 'react';

function SeriesList({ title }) {
  return (
    <section className="series-list">
      <h2>{title}</h2>
      <div className="series">
        {/* Ajoutez ici la liste des séries pour la catégorie */}
      </div>
    </section>
  );
}

export default SeriesList;
